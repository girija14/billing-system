# Database URL (SQLite default - no extra setup needed)
DATABASE_URL=sqlite:///./billing.db

# Email Configuration (optional - if not configured, invoices are logged to console)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=
